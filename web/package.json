{
  "name": "vulnassess-web",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "postinstall": "node -e \"if(process.platform!=='win32'){const fs=require('fs');const p='./node_modules/.bin/react-scripts';if(fs.existsSync(p)){fs.chmodSync(p,0o755);}}\""
  },
  "browserslist": {
    "production": [">0.2%", "not dead"],
    "development": ["last 1 chrome version"]
  }
}
